# Memory Backend

- Only has public API `openMemoryDB` of type `OpenDB`.
- Uses native TypeScript `Map` for store implementation.
- All methods are synchronous.
- Database `meta` uses plain TypeScript object.
