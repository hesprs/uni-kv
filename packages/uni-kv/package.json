{
	"name": "uni-kv",
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"build": "tsdown",
		"inspect": "MODE=inspect tsdown",
		"lint": "bun oxlint --fix --type-aware && bun oxfmt",
		"test": "vitest run",
		"check": "bun oxlint --type-aware && bun oxfmt --check"
	}
}
